<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:H.Presenters.Design.Controls">
    <Style x:Key="{ComponentResourceKey ResourceId=S.TextBlock.Binding, TypeInTargetAssembly={x:Type local:DesignTextBlockKeys}}" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{Binding Foreground}" />
        <Setter Property="FontSize" Value="{Binding FontSize}" />
        <Setter Property="Text" Value="{Binding Text}" />
        <Setter Property="FontFamily" Value="{Binding FontFamily}" />
        <Setter Property="FontStretch" Value="{Binding FontStretch}" />
        <Setter Property="FontWeight" Value="{Binding FontWeight}" />
        <Setter Property="FontStyle" Value="{Binding FontStyle}" />
        <!--<Setter Property="Opacity" Value="{Binding Opacity}" />
        <Setter Property="Height" Value="{Binding Height}" />
        <Setter Property="Visibility" Value="{Binding IsVisible, Converter={x:Static Converter.GetTrueToVisible}}" />
        <Setter Property="IsEnabled" Value="{Binding IsEnabled}" />
        <Setter Property="HorizontalAlignment" Value="{Binding HorizontalAlignment}" />
        <Setter Property="VerticalAlignment" Value="{Binding VerticalAlignment}" />-->
        <Style.Triggers>
            <DataTrigger Binding="{Binding Foreground}" Value="{x:Null}">
                <Setter Property="Foreground" Value="{DynamicResource {x:Static BrushKeys.Foreground}}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>