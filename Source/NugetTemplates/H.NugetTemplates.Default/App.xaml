<ApplicationBase x:Class="H.NugetTemplates.Default.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:H.NugetTemplates.Default">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <FontSizeTheme Type="{x:Static FontSizeThemeType.Default}" />
                <LayoutTheme Type="{x:Static LayoutThemeType.Default}" />
                <ColorTheme Type="{x:Static ColorThemeType.Dark}" />
                <ConciseStyle/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</ApplicationBase>
