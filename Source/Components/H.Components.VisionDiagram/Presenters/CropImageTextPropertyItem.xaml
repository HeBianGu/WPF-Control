<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
                    xmlns:local="clr-namespace:H.Components.VisionDiagram.Presenters">
    <DataTemplate DataType="{x:Type local:CropImageTextPropertyItem}">
        <Border BorderBrush="{DynamicResource {x:Static BrushKeys.BorderBrush}}"
                BorderThickness="1"
                CornerRadius="2">
            <Grid>
                <FontIconTextBlock x:Name="icon"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   FontSize="200"
                                   Opacity="0.6"
                                   Text="{x:Static FontIcons.Photo2}"
                                   Visibility="Hidden" />
                <Image Width="200"
                       Height="200"
                       HorizontalAlignment="Center"
                       Source="{Binding Value, Converter={GetImageSourceFromBase64Converter DecodePixel=300}}"
                       Stretch="Uniform" />
                <DockPanel HorizontalAlignment="Right"
                           VerticalAlignment="Top">
                    <Button Command="{Binding DeleteCommand}"
                            Content="删除" />
                    <!--<Button Command="{Binding CropCommand}"
                            Content="绘制" />-->
                </DockPanel>

            </Grid>
        </Border>
        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding Value}"
                         Value="{x:Null}">
                <Setter TargetName="icon" Property="Visibility" Value="Visible" />
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>
</ResourceDictionary>