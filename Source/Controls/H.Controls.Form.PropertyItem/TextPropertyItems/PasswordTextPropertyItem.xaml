<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:H.Controls.Form.PropertyItem.TextPropertyItems">
    <DataTemplate DataType="{x:Type local:PasswordTextPropertyItem}">
        <PasswordBox Height="Auto"
                     MinHeight="{DynamicResource {x:Static LayoutKeys.ItemHeight}}"
                     Margin="0,0,0,1"
                     VerticalContentAlignment="Center"
                     Cattach.Password="{Binding Value,Mode=TwoWay}"
                     ToolTip="{Binding Message}" />
    </DataTemplate>
</ResourceDictionary>