<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:local="clr-namespace:H.Controls.Form.PropertyItem.PropertyItems"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DataTemplate DataType="{x:Type local:OpenFileDialogPropertyItem}">
        <TextBox Height="Auto"
                 MinHeight="{DynamicResource {x:Static LayoutKeys.ItemHeight}}"
                 Margin="0,0,0,1"
                 VerticalContentAlignment="Center"
                 IsReadOnly="{Binding ReadOnly}"
                 Text="{Binding Value, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                 TextWrapping="{Binding TextWrapping}"
                 ToolTip="{Binding Message}" />
    </DataTemplate>
</ResourceDictionary>