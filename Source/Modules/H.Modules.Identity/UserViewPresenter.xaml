<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Behaviors="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:h="https://github.com/HeBianGu"
    xmlns:p="clr-namespace:H.Modules.Identity.Properties"
    xmlns:local="clr-namespace:H.Modules.Identity">
    <DataTemplate DataType="{x:Type local:UserViewPresenter}">
        <DockPanel MinWidth="900">
            <RepositoryGrid Type="{x:Type local:hi_dd_user}">
                <h:Cattach.CaptionRightTemplate>
                    <ControlTemplate>
                        <BulletDecorator Margin="5,0">
                            <BulletDecorator.Bullet>
                                <TextBlock VerticalAlignment="Center" Text="{Binding RelativeSource={RelativeSource AncestorType=ContentPresenter}, Path=DataContext.Value}" />
                            </BulletDecorator.Bullet>
                            <h:SelectionFilterBox
                                x:Name="sfb"
                                VerticalAlignment="Center"
                                Datas="{Binding Collection.Cache}"
                                PropertyName="Role"
                                Type="{x:Type local:hi_dd_user}">
                                <Behaviors:Interaction.Triggers>
                                    <Behaviors:EventTrigger EventName="FilterChanged">
                                        <Behaviors:ChangePropertyAction
                                            PropertyName="Filter1"
                                            TargetObject="{Binding Collection}"
                                            Value="{Binding ElementName=sfb, Path=Filter}" />
                                    </Behaviors:EventTrigger>
                                </Behaviors:Interaction.Triggers>
                            </h:SelectionFilterBox>
                        </BulletDecorator>
                    </ControlTemplate>
                </h:Cattach.CaptionRightTemplate>
                <Behaviors:Interaction.Triggers>
                    <Behaviors:EventTrigger EventName="Loaded">
                        <Behaviors:InvokeCommandAction Command="{Binding LoadedCommand}" />
                    </Behaviors:EventTrigger>
                </Behaviors:Interaction.Triggers>
                <DataGrid.Columns>
                    <DataGridTextColumn
                        Width="Auto"
                        Binding="{Binding Model.ID}"
                        Header="{x:Static p:Resources.ID}" />
                    <DataGridTextColumn
                        Width="*"
                        Binding="{Binding Model.Name}"
                        Header="{x:Static p:Resources.UserName}" />
                    <DataGridTextColumn
                        Width="*"
                        Binding="{Binding Model.Account}"
                        Header="{x:Static p:Resources.Account}" />
                    <DataGridTemplateColumn Width="*" Header="{x:Static p:Resources.Password}">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Margin="0,5" Text="******" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn
                        Width="*"
                        Binding="{Binding Model.DisplayName}"
                        Header="{x:Static p:Resources.DisplayName}" />
                    <DataGridCheckBoxColumn
                        Width="Auto"
                        Binding="{Binding Model.Enable}"
                        ElementStyle="{x:Null}"
                        Header="{x:Static p:Resources.Enable}"
                        IsReadOnly="True" />
                    <DataGridTextColumn
                        Width="*"
                        Binding="{Binding Model.Mail}"
                        Header="{x:Static p:Resources.Mail}" />
                    <DataGridTextColumn
                        Width="*"
                        Binding="{Binding Model.Role}"
                        Header="{x:Static p:Resources.Role}" />
                    <DataGridTemplateColumn Width="*" Header="{x:Static p:Resources.Authors}">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ItemsControl ItemsSource="{Binding Model.Role.Authors}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Margin="0,5" Text="{Binding Name}" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </RepositoryGrid>
        </DockPanel>
        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding IsVisible}" Value="False">
                <Setter Property="Visibility" Value="Collapsed" />
            </DataTrigger>
            <DataTrigger Binding="{Binding IsAuthority}" Value="False">
                <Setter Property="Visibility" Value="Collapsed" />
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>
</ResourceDictionary>