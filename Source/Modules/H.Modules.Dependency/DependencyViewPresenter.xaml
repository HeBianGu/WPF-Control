<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:h="https://github.com/HeBianGu"
    xmlns:local="clr-namespace:H.Modules.Dependency">
    <DataTemplate DataType="{x:Type local:DependencyViewPresenter}">
        <Grid>
            <ItemsControl ItemsSource="{Binding Source={x:Static local:DependencyOptions.Instance},Path=DependencyItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StaticForm SelectObject="{Binding}" UsePropertyView="True"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding IsVisible}" Value="False">
                <Setter Property="Visibility" Value="Collapsed" />
            </DataTrigger>
            <DataTrigger Binding="{Binding IsAuthority}" Value="False">
                <Setter Property="Visibility" Value="Collapsed" />
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>
</ResourceDictionary>