<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:H.Styles.Default">
    <Style TargetType="{x:Type local:FontIcon}">
        <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
        <Setter Property="Text" Value="&#xE700;" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <DataTemplate DataType="{x:Type local:IconSegoe}">
        <GroupBox Header="{Binding Key}">
            <Grid>
                <local:FontIcon
                    Margin="10"
                    FontSize="120"
                    Text="{Binding Value}" />
                <Button
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Command="{Binding CopyCommand}"
                    Content="{Binding Key}" />
                <Button
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom"
                    Command="{Binding CopyCodeKeyCommand}"
                    Content="{Binding CodeKey}" />
            </Grid>
        </GroupBox>
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:IconSegoes}">
        <ListBox ItemsSource="{Binding}" Style="{DynamicResource {x:Static ListBoxKeys.Default}}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ListBox>
    </DataTemplate>
</ResourceDictionary>