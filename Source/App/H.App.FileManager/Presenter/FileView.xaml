<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
                    xmlns:h="https://github.com/HeBianGu"
                    xmlns:local="clr-namespace:H.App.FileManager">

    <DataTemplate DataType="{x:Type local:fm_dd_file}">
        <Image Width="40"
               Height="40"
               Margin="15"
               Source="{Binding Url, Converter={h:GetFilePathToSystemInfoIconConverter}, IsAsync=True}" />
    </DataTemplate>

    <DataTemplate DataType="{x:Type local:FileView}">
        <DockPanel>
            <Image Width="100"
                   Height="100"
                   Margin="10"
                   HorizontalAlignment="Left"
                   DockPanel.Dock="Top"
                   Source="{Binding Model.Url, Converter={h:GetFilePathToSystemInfoIconConverter}, IsAsync=True}" />
            <h:Form SelectObject="{Binding}" UsePropertyView="True" />
        </DockPanel>
    </DataTemplate>
</ResourceDictionary>